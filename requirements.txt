# photo_organizer requirements
# Install with: pip install -r requirements.txt

PyYAML>=6.0                    # core/ (obsidian_core) — frontmatter serialization
PyQt6>=6.6                     # ui/ (photo_organizer_gui.py) — Phase 2 UI
Pillow>=10.0
piexif>=1.1.3
reverse_geocoder>=1.5.1
face_recognition>=1.3.0       # requires cmake + dlib (see README)
numpy>=1.24
transformers>=4.35             # CLIP model
torch>=2.0
torchvision>=0.15
imagehash>=4.3          # perceptual hashing for near-duplicate detection
